# TB_CPA_Harmonize v1.2 dependencies
# Install with: pip install -r requirements.txt

# ── Core pipeline ──────────────────────────────────────────────────────────────
pandas >= 2.3.0
openpyxl >= 3.1.5       # Excel read/write (trace log)
pyarrow >= 20.0.0
tabulate >= 0.9.0
python-magic >= 0.4.27
python-magic-bin >= 0.4.14  # Windows DLL for python-magic
tqdm >= 4.0.0
pyyaml >= 6.0

# ── GUI ───────────────────────────────────────────────────────────────────────
PySide6                  # harmonize_gui.py
