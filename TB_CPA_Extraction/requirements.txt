# TB_CPA_Extraction v1.2 dependencies
# Install with: pip install -r requirements.txt

# ── Core pipeline (same as TB_CPA_Extraction) ─────────────────────────────────
patool           # archive detection & extraction
pandas
openpyxl         # Excel read/write (trace log)
pyyaml           # format_config.yaml
python-magic     # file type detection
tqdm             # progress bars
tabulate         # console table summary

# ── GUI ───────────────────────────────────────────────────────────────────────
PySide6          # extraction_gui.py

# ── Optional (already used by pipeline) ───────────────────────────────────────
# winsound — Windows built-in, no install needed
